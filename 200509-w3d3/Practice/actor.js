class Actor {
    constructor(){
        // console.log("Actor is online");
        this.type = null; 
        
    }

    setType (type) {
        // console.log("set type of an actor to: " +type);
        this.type = type;
    }
}